import { useContext } from "react"
import { changeUser } from "../../../server/Database/Database"
import ChangeUser from "./ChangeUser"

export default function useChange() {

    const {} = useContext(changeUser)

  return (
    
  )
}
